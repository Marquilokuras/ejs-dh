<!DOCTYPE html>
<html>

<head>
  <title>Pimienta & sal</title>
  <link rel="stylesheet" href="/css/style.css" />
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
</head>

<body>
  <!-- Navbar (sit on top) -->
  <nav class="ps-top">
    <div class="ps-bar ps-white ps-padding ps-card ps-nav">
      <a href="#home" class="ps-bar-item ps-button">Pimienta & Sal</a>
      <!-- Right-sided navbar links. Hide them on small screens -->
      <div class="ps-right ps-hide-small">
        <a href="#about" class="ps-bar-item ps-button">About</a>
        <a href="#menu" class="ps-bar-item ps-button">Menu</a>
      </div>
    </div>
  </nav>

  <!-- Header -->
  <%- include('./partials/header.ejs') %>

    <!-- Page content -->
    <div class="ps-content">

      <!-- Menu Section -->
      <div class="ps-row ps-padding-64" id="menu">
        <div class="ps-col l6 ps-padding-large">
          <h1 class="ps-center">
            <%= platoBuscado?.nombre %>
          </h1>
          <br />
          <%if(platoBuscado?.description){ %>
            <h4>
              <%= platoBuscado?.descripcion %>
            </h4>
            <% }else{ %>
              <h4>Plato Tipico</h4>
              <%}%>
                <p class="ps-text-grey">
                  U$S <%= platoBuscado?.precio%>
                </p>
        </div>

        <div class="ps-col l6 ps-padding-large ps-img-menu">
          <img src="/images/<%= platoBuscado?.imagen %>" class="ps-round ps-image ps-opacity-min"
            alt="<%= platoBuscado?.nombre %>" />
        </div>

      </div>

      <!-- Footer -->
      <%- include('./partials/footer.ejs') %>
    </div>
</body>

</html>